/// <reference types="@cloudflare/workers-types" />

// Ensure Cloudflare Worker types are available globally
declare global {
  // These types are provided by @cloudflare/workers-types
  // but we're declaring them globally for better IDE support
}

export {};

